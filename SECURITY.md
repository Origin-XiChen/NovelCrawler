# 安全策略(Security Policy)

## 支持的版本

| 版本 | 状态 |
|------|------|
| 0.19.x | ✅ 接受安全报告 |
| < 0.19 | ❌ 请先升级 |

## 如何报告漏洞

**请勿在公开 Issue 中描述可被利用的漏洞细节。**

推荐方式:使用 GitHub 的 **私有安全咨询**(仓库页 → Security → Advisories → New draft security advisory),仅项目维护者可见。

报告时请尽量包含:

- 影响的文件/接口(如 `gui_server.py` 的某个路由)
- 复现步骤或最小化 PoC
- 影响评估(数据泄露 / 代码执行 / 拒绝服务)
- 你的运行环境(Windows 版本、浏览器)

我们力争在 **72 小时内**首次响应,确认后尽快修复并发布补丁版本;报告者可选择是否在公告中被致谢。

## 范围界定

**属于本项目的安全问题**,例如:

- `gui_server.py` HTTP 路由的路径遍历、SSRF、XSS、请求走私
- 本地服务未授权访问导致的数据泄露(书架/下载文件/设置)
- 打包产物(exe)中意外包含敏感信息
- WebRTC / MQTT 信令链路中破坏端到端假设的问题

**不属于本项目范围**:

- 各书源/漫画源目标站点自身的安全或内容问题
- 用户在本机运行的第三方隧道客户端(natapp/cpolar/frp)自身的漏洞
- 对工具进行修改后引入的问题

## 已知设计取舍(使用前请知悉)

- 本程序是**本地个人工具**:HTTP 服务默认监听 `0.0.0.0` 以便手机局域网访问,**没有身份鉴权**——请仅在可信网络中使用;不可信网络下请用 `--host 127.0.0.1` 启动并关闭手机相关功能。
- 「公网穿透」「任意网络直连」属**用户显式开启**的高级功能:开启即表示你理解流量将经过选定的第三方隧道服务或公共 MQTT 信令节点(信令内容经派生密钥处理,但隧道中转的页面访问属明文 HTTP)。
- 下载的书源规则指向第三方站点,其返回内容不受本项目控制,请保持 `verify_ssl` 开启并对可疑站点停用。
